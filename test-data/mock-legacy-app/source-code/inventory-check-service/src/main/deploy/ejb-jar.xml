<?xml version="1.0" encoding="UTF-8"?>
<!-- SYNTHETIC legacy code — fictional portfolio test data, not a real system. -->
<ejb-jar>
  <enterprise-beans>
    <session>
      <ejb-name>InventoryCheckSessionBean</ejb-name>
      <home>com.ordervault.inventory.InventoryCheckHome</home>
      <remote>com.ordervault.inventory.InventoryCheckRemote</remote>
      <ejb-class>com.ordervault.inventory.InventoryCheckSessionBean</ejb-class>
      <session-type>Stateless</session-type>
      <transaction-type>Container</transaction-type>
      <resource-ref>
        <res-ref-name>jdbc/OrderVaultDS</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
      </resource-ref>
    </session>
  </enterprise-beans>

  <assembly-descriptor>
    <container-transaction>
      <method>
        <ejb-name>InventoryCheckSessionBean</ejb-name>
        <method-name>reserveStock</method-name>
      </method>
      <trans-attribute>Required</trans-attribute>
      <!--
        NOTE: default WebLogic transaction timeout applies here (30s) —
        no custom <trans-timeout-seconds> override exists for this
        specific method, despite it being the slowest/most contention-prone
        operation in the system. Candidate for review during migration.
      -->
    </container-transaction>
  </assembly-descriptor>
</ejb-jar>
